整个配置编辑页面的显示中的输入框风格统一，不要一长一短，请按照现代化的设计进行修改
当前界面中对于较多设置的页面需要滚动条。
支持深色和浅色主题，确保显示协调，风格统一。
当前界面中对于较多设置的页面需要滚动条。
dashboard 中的 Recent Health Checks 中就没有滚动条，
还有滚动条你看是否需要在有必要时显示
模型选择输入框放到 endpoint 写好周进行选择获取，Effort 也是
配置表单中 Agent Type 去除，将 Model Provider 前置到原 Agent Type 位置。
HTTP 代理相关的配置放到最低下。
优化配置列表点击后的详细信息显示，目前显示过于单调，不符合界面设计

架构上的问题，语法上的问题，性能上的问题，冗余问题

支持本地化，在设置中支持语言的调整，支持中文和英文切换

目前深色背景中存在按钮之类的与背景颜色重合，黑色背景应该使用白色icon，请修改，需要符合界面设计

程序为单实例，不能启动多个

设置界面中的侧边栏选项过于小，不协调，请修改。

设置界面中程序选项内的 Reduce Motion 移除。
Accent Color 属性生效。
配置删除需要二次确认。
Health Checks 不能是每一次都进行检查，
合理的逻辑应该对用户的所有已有的配置进行检查，而不是每点击一次就进行一次列表的累加。

业务逻辑，项目在启动时会在项目所在路径创建一个 loomprofile 文件夹，
当用户新增一个配置，需要在文件夹内新建一个文件夹，
文件夹的名字由用户的对 profile 的名字决定，所以在新建 profile 一定要有名字，如果没有名字一定要提醒用户进行警告。
正常在用户配置好之后进行保存，
保存到目录中的有三个文件，一个是 .env、一个是 config.toml、一个是 auth.json
当用户对这个配置进行应用的时候，将用户根目录下的 .codex 内的对应的配置文件进行替换。

对于token的通过与使用量需要到中找到对应的 session 日期进行统计，
单次对话的 session 统计文件大概这样 `
/home/cells/dev/loom/rollout-2026-05-15T11-09-33-019e299c-5bc4-7be2-a4b5-5955eb5bee85.jsonl`
从这个里面进行统计


配置进行持久化，将用户的选择进行持久化的存储至loomprofile中，配置文件名称为setting.json文件，包含用户的配置选项，用户的设置
